var vows = require('vows'),
    assert = require('assert'),
    crawler = require('../../crawler/main').core;

vows.describe({
    'Scheduler index': {
        topic: '',

        'routine': function ( topic ) {
            // test routine
        }
    }    
}).run();

#!/usr/bin/env node

var Scheduler = (function () {
    var core = function () {};

    core.prototype = {
        routine: function () {
            // Routine of indexation      
        },
    };

    return new core;
} ());

exports.core = Scheduler;
